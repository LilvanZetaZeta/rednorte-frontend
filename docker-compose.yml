services:
  app:
    build: .
    container_name: rednorte-frontend-app
    ports:
      - "5173:5173"
    restart: unless-stopped
    networks:
      - network-RedNorte

networks:
  network-RedNorte:
    external: true
